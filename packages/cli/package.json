{
  "name": "@qnotes/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "qnotes": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "qnotes": "node dist/index.js",
    "test": "pnpm build && node --test test/*.test.mjs"
  },
  "dependencies": {
    "@qnotes/api-client": "workspace:*",
    "@qnotes/shared": "workspace:*"
  }
}
